# Hero media

- **`hero-ambient.mp4`** — looping video in the **home hero right-hand frame** when no hero image is set (`home.hero.visual.src` is null). Path is wired in `content.ts` as `visual.videoSrc`. For production, consider a smaller encode (e.g. 1080p) for faster load.
